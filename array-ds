#include<bits/stdc++.h>
#include<vector>
using namespace std;
int main()
{   int n,t,i;
    cin>>n;
    vector<int>a;
    
    for( i=0;i<n;i++)
    { 
        cin>>t;
        a.push_back(t);
        
    }
    reverse(a.begin(),a.end());
    for(i=0;i<n;i++)
    cout<<a[i]<<" ";
}
